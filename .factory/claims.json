[
  {
    "id": "sample-preflight",
    "claim": "One click opens three sample events with invitation, timezone, repeat, attendee, alarm, link, and duplicate findings.",
    "where": "landing action and demo",
    "test": "npm test -- --grep @claim:sample-preflight",
    "sandbox": "fresh browser context at /; click the sample action and assert all seven listed finding families"
  },
  {
    "id": "event-preview",
    "claim": "A chosen calendar file shows every event title and start date in Event preview.",
    "where": "README and page metadata",
    "test": "npm test -- --grep @claim:event-preview",
    "sandbox": "fresh browser context; upload a clean two-event ICS fixture and assert each title plus its visible start-date time element in Event preview"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo uses sample data and does not change a saved real file.",
    "where": "demo banner, Privacy, README",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "seed a real IndexedDB record; exercise reset, reload, Return, wordmark, Back, Privacy, and Terms exits; byte-compare before and after every resulting Forget action"
  },
  {
    "id": "local-only",
    "claim": "Event details stay in this browser, and embedded links are not opened.",
    "where": "landing facts, intake note, Privacy, README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh context at /demo; reveal a sample embedded link while observing all requests and URL state"
  },
  {
    "id": "repair-export",
    "claim": "Selected fixes change only a downloaded copy: they add missing IDs and UTC stamps, remove people, alarms, and invitation mode, or write CRLF lines. People and alarm blocks stay unless selected.",
    "where": "landing facts, repair deck, boundaries, Terms, README",
    "test": "npm test -- --grep @claim:repair-export",
    "sandbox": "save a real fixture; apply every repair; inspect each named output change and CRLF lines; byte-compare IndexedDB plus reloaded raw source"
  },
  {
    "id": "risk-detection",
    "claim": "Checks structure, IDs, times, timezones, repeat rules, duplicates, invitation modes, people, alarms, and links.",
    "where": "README and inspection findings",
    "test": "npm test -- --grep @claim:risk-detection",
    "sandbox": "malformed in-memory fixture; assert a finding for each advertised risk family"
  },
  {
    "id": "calendar-export",
    "claim": "Exports a checked ICS copy for Apple Calendar, Google Calendar, or Outlook.",
    "where": "export deck and README",
    "test": "npm test -- --grep @claim:calendar-export",
    "sandbox": "clean in-memory ICS fixture; select each calendar app and assert filename plus destination output"
  },
  {
    "id": "paste-intake",
    "claim": "Pasted ICS text is checked and saved like a chosen calendar file.",
    "where": "paste intake and How the checker works",
    "test": "npm test -- --grep @claim:paste-intake",
    "sandbox": "fresh context; paste a valid ICS fixture, submit, and assert populated workspace plus IndexedDB record"
  },
  {
    "id": "local-restore",
    "claim": "The latest real file survives refresh until the user forgets it.",
    "where": "Privacy and README",
    "test": "npm test -- --grep @claim:local-restore",
    "sandbox": "fresh browser storage; upload clean fixture, refresh, forget, and refresh again"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts and README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; wait for service worker, disable network, reload"
  },
  {
    "id": "no-third-party-runtime",
    "claim": "It uses no analytics, advertising, third-party scripts, or remote fonts.",
    "where": "Privacy and README",
    "test": "npm test -- --grep @claim:no-third-party-runtime",
    "sandbox": "fresh context; visit every product route, inspect runtime script/style origins, and require only same-origin requests"
  }
]
